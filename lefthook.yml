pre-commit:
  parallel: true
  jobs:
    - name: biome-check
      run: pnpm exec biome check --no-errors-on-unmatched {staged_files}
      glob:
        - "*.ts"
        - "*.tsx"
        - "*.js"
        - "*.jsx"
        - "*.json"
      stage_fixed: true
